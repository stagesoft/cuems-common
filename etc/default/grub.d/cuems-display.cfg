# CUEMS display kernel parameters
# Disables Intel display power C-states (DC states) to fix HDMI output
# initialization failures on warm restart with Alder Lake-N and similar
# Intel platforms that route HDMI through DP++ controllers.
GRUB_CMDLINE_LINUX_DEFAULT="$GRUB_CMDLINE_LINUX_DEFAULT i915.enable_dc=0"
